#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

int main() {
  int n, tot;
  vector<int> s;
  cin >> tot >> n;
  for (int i = 0; i < n; i++) {
    int x;
    cin >> x;
    tot.push_back(x);
  }

  int[2n][n] dp;

  for (int i = 0; i < n; i++) {
    
  }
}
